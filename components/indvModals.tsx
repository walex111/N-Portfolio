import AboutModal from "./modals/aboutModal";
import ContactModal from "./modals/ContactModal";
import ProjectsModal from "./modals/projectsModal";

const keyPageRoute = [
  {
    About: `<${AboutModal}/>`,
    Contact: `<${ContactModal}/>`,
    Projects: `<${ProjectsModal}/>`,
  },
];

const IndvModals = () => {
  return (
    <>
    
    </>
  )
};

export default IndvModals;
